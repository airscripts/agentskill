# Guidance Fixtures

These fixtures cover document creation, maintenance, signatures, provenance,
custom instructions, and declined maintainer questions. They are inputs for
validation and skill-workflow tests; they are not generated guidance for this
repository.

The `scoped/` fixture also covers nested scope metadata, independent free
regions, signatures, and informational missing-scope suggestions.
